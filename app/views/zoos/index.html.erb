<p id="notice"><%= notice %></p>

<h1>Zoo database</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Species</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @zoos.each do |zoo| %>
      <tr>
        <td><%= zoo.species %></td>
        <td><%= zoo.name %></td>
        <td><%= link_to 'Show', zoo %></td>
        <% if user_signed_in? %>
          <td><%= link_to 'Edit', edit_zoo_path(zoo) %></td>
          <td><%= link_to 'Destroy', zoo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% else  %>
        	
        <% end %>
        
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% if user_signed_in? %>
  <%= link_to 'New Specie', new_zoo_path %>
<% else  %>
	
<% end %>

